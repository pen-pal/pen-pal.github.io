---
import { profile } from '../data/profile';
---

<div class="marquee-wrap">
  <div class="marquee">
    {profile.marquee.map(m => <span>{m}</span>)}
    {profile.marquee.map(m => <span>{m}</span>)}
  </div>
</div>
